- from:
    uri: "timer:stock"
    parameters:
      period: 5000
    steps:
      - log: |


          Stock prices today

          ${stock()}
          ${stock('AAPL')}
          ${stock('IBM')}

          Have a nice day
        